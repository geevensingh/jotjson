/**
 * Shared client-side constants matching the rule-set API contracts. Mirrored
 * by api/src/rule-sets/* and DESIGN_SPEC.md §Features 7. Hoisted out of
 * `rule-editor.component.ts` so the list page (M6e) can validate without
 * importing from the editor module.
 */
export const NAME_MAX = 80;
